"""Reserved package for future application helpers.

The previous PostgreSQL/psycopg connection-pool helpers were removed because
this project now uses Django's built-in SQLite database exclusively.
"""
